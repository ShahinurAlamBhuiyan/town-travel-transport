import React from 'react';
import Header from '../Header/Header';

const Destination = () => {
    return (
        <div>
            <Header/>
            <h2>This is destination components</h2>
        </div>
    );
};

export default Destination;